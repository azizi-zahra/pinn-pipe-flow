# =============================================================================
# exp_024_physics_weight_100.yaml -- tests physics weight of 100.0.
# All settings are inherited from base.yaml unless overridden below.
# =============================================================================

metadata:
  name: "exp_024_physics_weight_100"
  description: "Test physics loss weight of 100.0 with best architecture (depth=6, 64000 epochs)."

overrides:
  model:
    hidden_layer_depth: 6
  training:
    epochs: 64000
    loss_weight_physics: 100.0
