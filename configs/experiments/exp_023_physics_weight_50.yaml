# =============================================================================
# exp_023_physics_weight_50.yaml -- tests physics weight of 50.0.
# All settings are inherited from base.yaml unless overridden below.
# =============================================================================

metadata:
  name: "exp_023_physics_weight_50"
  description: "Test physics loss weight of 50.0 with best architecture (depth=6, 64000 epochs)."

overrides:
  model:
    hidden_layer_depth: 6
  training:
    epochs: 64000
    loss_weight_physics: 50.0
