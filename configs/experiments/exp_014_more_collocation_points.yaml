# =============================================================================
# exp_014_more_collocation_points.yaml -- tests more collocation points.
# All settings are inherited from base.yaml unless overridden below.
# =============================================================================

metadata:
  name: "exp_014_more_collocation_points"
  description: "Tests more collocation points compared to baseline."

overrides:
  model:
    hidden_layer_depth: 6
  training:
    epochs: 64000
  sampling:
    num_interior_points: 5000